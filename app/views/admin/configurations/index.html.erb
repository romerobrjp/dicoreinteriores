<% content_for :page_header, 'Configurações' %>
<div class="box box-primary">
  <div class="box-header with-border">
    <h3 class="box-title">Configurações</h3>
    <div class="box-tools pull-right">
      <div class="btn-group btn-group-sm">
        <%= link_to t('views.admin.submenu.new_configuration'), new_admin_configuration_path, class: 'btn btn-primary' %>
      </div>
    </div>
  </div>

  <div class="box-body">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Cor 1</th>
          <th>Cor 2</th>
          <th>Cor 3</th>
          <th>Cor 4</th>
          <th>Cor 5</th>
          <th></th>
        </tr>
      </thead>

      <tbody>
        <% @categories.each do |configuration| %>
        <tr>
          <td><%= configuration %></td>
          <td>
            <div class="btn-group btn-group-xs" role="group">
              <%= link_to edit_admin_configuration_path(configuration), class: 'btn btn-warning' do %>
              <i class="fa fa-pencil"></i><% end %>
              <%= link_to admin_configuration_path(configuration), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' do %>
              <i class="fa fa-trash"></i><% end %>
            </div>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>

  </div>
</div>
