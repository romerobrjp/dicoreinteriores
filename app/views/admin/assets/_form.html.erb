<%= simple_form_for([:admin, @asset]) do |f| %>
  <%= f.error_notification %>

  <div class="box-body">
    <div class="row">
      <div class='col-md-8'>
        <%= f.input :description %>
      </div>
    </div>

    <div class="row">
      <div class='col-md-8'>
        <%= f.input :file %>
      </div>
    </div>
  </div>

  <div class="box-footer">
    <%= f.button :submit %>
  </div>
<% end %>
