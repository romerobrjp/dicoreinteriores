<% content_for :page_header, t('views.admin.preferences.title') %>
<div class="box box-primary">
  <div class="box-header with-border">
    <h3 class="box-title"> <%= t('views.admin.preferences.edit') %> </h3>
  </div>
  <div class="box-body">
    <%= render 'form' %>
  </div>
</div>
